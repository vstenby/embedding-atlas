{
  "scripts": {
    "dev": "vitepress dev",
    "build": "npm run generate_assets && vitepress build --base /embedding-atlas/",
    "preview": "npm run generate_assets && vitepress preview",
    "generate_assets": "./generate_assets.sh"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "gh-pages": "^6.3.0"
  }
}
