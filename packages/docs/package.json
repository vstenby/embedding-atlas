{
  "scripts": {
    "dev": "vitepress dev",
    "build": "npm run generate_assets && vitepress build",
    "preview": "npm run generate_assets && vitepress preview",
    "deploy": "npm run generate_assets && vitepress build --base /embedding-atlas/ && gh-pages --nojekyll -d .vitepress/dist",
    "generate_assets": "./generate_assets.sh"
  },
  "devDependencies": {
    "vitepress": "^1.1.4",
    "gh-pages": "^6.1.1"
  }
}
